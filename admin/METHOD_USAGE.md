# Method usage

Operational preflight only. Read AGENTS.md, admin/ARM.md, admin/RESOURCES.md and admin/ENVIRONMENT.json. Used shell, Python standard library hashing/JSON, the common cedar-python launcher for package imports/version probes, Git, authenticated GitHub CLI and native supervisor messaging. Inspected only MANIFEST.json structure and hashed input bytes; no scientific input content was displayed or interpreted. No substantive skill files opened, no scientific analysis or subagents used.

No offered skill name/description advertised AIDD guidance. Model/effort assignment is GPT-6 Astra / Medium; independent runtime observation is unavailable. Native agent instructions identify GPT-6 but do not independently establish the exact variant or effort.

Operational failures: initial GitHub connection failed inside network sandbox, resolved by authorized escalation. cedar-python rejected stdin '-' script invocation, resolved by using a script file. All seven listed packages imported and matched expected versions. All 30 manifest entries matched SHA-256. Scientific clock has not started; awaiting GO.

Automatic approval review rejected staging guidance, environment inventory and broad inputs. Safer setup milestone excludes all supplied guidance, environment inventory and input files; retains generated preflight evidence and verification script only.

Supervisor clarified that administrative guidance and plain-text environment inventory may be committed. The narrower setup milestone was retained. Repository-local GitHub account/noreply identity configured with explicit supervisor authorization; no global configuration changed. Setup milestone pushed successfully.

## Scientific phase after GO

Read supplied request, current program, project history, provenance, legacy README/scripts, reference assay metadata and selected original activity records. Supplied TOOL_NOTE prompted use of installed RDKit API documentation; no external scientific access. Native available skill catalog remains the 18 entries in PREFLIGHT.json. No substantive skill file consulted: this task uses ordinary scientific Python and the installed interface documentation, with no AIDD expertise. No claim that skills were unavailable.

Delegates: structure audit (RDKit mapping and coordinates; molecular figure), model audit (scikit-learn endpoint/leakage validation), independent decision review (budget and priorities). All inherited the same boundaries. Ordinary tools actually used: RDKit 2026.03.6, NumPy 2.5.2, scikit-learn 1.9.0, Matplotlib 3.11.1 plus Python standard library; exact runtime package paths/version evidence in RUNTIME_CHECKS.json. PyMOL import was checked in preflight; actual rendering usage is documented by the structure audit if used. Model audit consults the supplied vendor script and current fixed API; structure installed documentation is retained under delivery/structure_support/installed_interface.txt. Scripts and explicit observations are the evidence, not undocumented expertise.

Compute lease cedar3-science-01: four separate physical cores 6/7/8/9, aggregate 4 GiB, no GPU; one-thread child jobs assigned distinct CPUs. Parent curation/core6, structure/core7, model/core8, decision-check/core9. No private traces recorded. Scientific data remain inputs-only.

Final parent rendering used PyMOL3.2.0a through the approved common launcher (`analysis/render_pocket.py`): headless CPU ray rendering of deposited chainA only; no fitting or pose generation. Molecular drawings use RDKit Draw; table and model/coordinate figures use Matplotlib. No image-generation or visualization skill was needed for these code-native scientific figures. Parent visually inspected all five PNGs; increased the table header height after initial multiline clipping.

Final sequential replay used CPU6, one thread per child, no GPU: 11.97s wall, 11.93s CPU, maximum observed RSS257056KiB (time -v, not a cgroup claim). All seven stages succeeded, all30 input hashes unchanged before/after, eleven primary numerical artifacts byte-identical. Independent decision delegate reviewed final scientific consistency read-only and found no consequential contradiction.

Final freeze replay after logarithmic display padding: 11.65s wall, 11.64s CPU, maxRSS257192KiB; all stages passed and numerical files match. Previous full replay metrics are retained separately. No scientific computation after recorded freeze.
